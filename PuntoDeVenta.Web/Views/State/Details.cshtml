@model PuntoDeVenta.Common.Entities.cState
@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<h1>@Model.nombre</h1>
<hr />
<div>

    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.id)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.id)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.nombre)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.nombre)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { id = Model.id }, new { @class = "btn btn-outline-primary", @style = "border-radius: 60px;" })
    <a asp-action="Index" class="btn btn-outline-secondary" style="border-radius: 60px;">Back to List</a>
</div>